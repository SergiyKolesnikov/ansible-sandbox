all:
  children:
    webservers:
      hosts:
        testserver:
          ansible_host: host02
          ansible_user: root
    local:
      hosts:
        localhost:
          ansible_connection: local